/*
 *
And the circus at the end of time has not stopped since Silly tried to reason with a wall.

*/

import { Elf } from '@silly/types'

const $ = Elf("hello-world")

$.draw(_ => {
  return "Hello, World!"
})

Elf($)
